// Typography

// Fonts
$font-sans = 'OlsenOT',Helvetica,Arial,sans-serif
$font-sans-light = 'OlsenOT-Light',Helvetica,Arial,sans-serif
$font-sans-bold = 'OlsenOT-Bold',Helvetica,Arial,sans-serif
$font-secondary = Helvetica,Arial,sans-serif
$font-serif = Georgia,serif
$primary-font-family = $font-sans
$secondary-font-family = $font-serif
$monospaced-font-family = Monaco, Lucida Console, monspace